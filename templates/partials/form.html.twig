{{ form_start(form) }}

    {{form_widget(form)}}
    <div class="d-grid gap-2 col-6 mx-auto">
    <input type="submit" value="{{ submitButtonText}}" class="btn btn-primary bg-gradien
 btn-block"/>
   </div>
 {{ form_end(form) }}