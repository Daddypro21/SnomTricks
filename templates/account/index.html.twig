{% extends 'base.html.twig' %}

{% block title %}Profil{% endblock %}

{% block body %}
    <div class="row profil">
        <div class="col-md-6  mx-auto text-center">
            <h1 class="my-5">Profil</h1>
            <img src="{{asset('images/about.jpg')}}" alt=" avatar de {{app.user.fullName()}}" class=" shadow rounded-circle ">

            <h2 class="mt-5">{{app.user.fullName()}}</h2>
            <p class="lead">{{app.user.email}}</p>
            <p ><small> compte créé le {{app.user.createdAt|date('d/m/Y')}}</small></p>
            <P>
            <a href="{{path('app_account_edit')}}" type="button" class="btn btn-secondary btn-sm ms-2 mb-2">Editer le compte </a> <a href="{{path('app_account_changepassword')}}" type="button" class="btn btn-secondary btn-sm ms-2 mb-2">Changer le mot de passe</a>
            </a>
        </div>
    </div>
{% endblock %}
